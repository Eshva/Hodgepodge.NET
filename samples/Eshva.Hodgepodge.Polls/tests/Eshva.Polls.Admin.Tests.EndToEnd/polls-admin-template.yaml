version: "3"
services:
#  admin-bff:
#    container_name: admin-bff
#    build:
#      context: {{insert-here-somehow-absolute-path-to-dockerfile-folder}}
  redis:
    container_name: redis
    image: redis:5-alpine
    restart: on-failure
    ports:
      - {{redisPort}}:6379
